# Copy this file to .env (for local development) and fill values.
# On Render (or other hosts) set these values in the service environment variables instead.

# Google Gemini API key (required)
GOOGLE_API_KEY=your-google-api-key-here

# Optional model override
MODEL=models/gemini-2.5-flash

# Supabase (optional — not actively used yet)
SUPABASE_URL=your-supabase-url-here
SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Compute / runtime settings
DEVICE=cpu
BATCH_SIZE=16
COMPUTE_TYPE=int8
WHISPER_MODEL=base
